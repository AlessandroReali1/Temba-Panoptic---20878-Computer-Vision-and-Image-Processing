_BASE_: "../youtubevis_2021/video_maskformer2_R50_bs16_8ep.yaml"

DATASETS:
  TRAIN: ("jrdb_panovideo_train",)
  TEST: ("jrdb_panovideo_val",)

MODEL:
  WEIGHTS: "/path/to/project/starting_checkpoint/mask2former_ytvis2021_r50.pkl"
  SEM_SEG_HEAD:
    NUM_CLASSES: 61

INPUT:
  SAMPLING_FRAME_NUM: 2
  SAMPLING_FRAME_RANGE: 20
  SAMPLING_FRAME_SHUFFLE: False

DATALOADER:
  NUM_WORKERS: 1

SOLVER:
  IMS_PER_BATCH: 1
  BASE_LR: 0.0001

OUTPUT_DIR: "/path/to/project/temba_project/output/jrdb_video_mask2former_baseline"
